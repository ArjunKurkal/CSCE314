(define list '((A B) (C D)))

(display list) ; 
(display " --> the list\n");

(display (car list)) ; 
(display " --> car\n");

(display (caar list)) ; 
(display " --> caar\n");

(display (cadr list)) ; 
(display " --> cadr\n");

(display (cadar list)) ; 
(display " --> cadar\n");

(display (caadr list)) ; 
(display " --> caadr\n");

(display (cadadr list)) ;
(display " --> cadadr");



